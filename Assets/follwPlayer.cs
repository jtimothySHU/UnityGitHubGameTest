using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class follwPlayer : MonoBehaviour
{

    public Transform player;

    /*
    // initialized in the unity window
    public Vector3 offset;

    // Update is called once per frame
    void Update()
    {
        // This logs our player's position in the console
        Debug.Log(player.position);

        // "transform" refers to the transformation of the current object associated w/ the script
        transform.position = player.position + offset;
    }
    */
    // commented out for now, will be useful once ball mechanics are implemneted
}
